{
  "name": "extensions-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "pm2 start server.js --name dashboard",
    "deploy": "pm2 restart dashboard",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.20.0",
    "puppeteer": "^24.39.0"
  },
  "devDependencies": {
    "@types/node": "^25.4.0"
  }
}